# @package _global_
defaults:
  - override /output_type: answer_only

dataset_choice: 
  _args_: ["math"]

output_type:
  train_batch_size:          8
  eval_batch_size:           16
  max_length:                1024

model_name_or_path: "Qwen/Qwen2.5-1.5B"
max_num_epochs: 4
learning_rate: 0.0001
