# @package _global_
defaults:
  - override /output_type: chain_of_thought_then_answer

dataset_choice: 
  _args_: ["math"]

output_type:
  train_batch_size:          1
  eval_batch_size:           1
  # max_length:              512

model_name_or_path: "HuggingFaceTB/SmolLM2-1.7B-Instruct"

use_peft: false
tok_max_total_length:      768

max_num_epochs:            15
learning_rate:             0.0005