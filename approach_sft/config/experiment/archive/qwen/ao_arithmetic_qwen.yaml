# @package _global_
defaults:
  - override /output_type: answer_only

dataset_choice: 
  _args_: ["arithmetic"]

output_type:
  train_batch_size:          18
  eval_batch_size:           18
  max_length:                170

model_name_or_path: "Qwen/Qwen2.5-1.5B"
max_num_epochs: 4