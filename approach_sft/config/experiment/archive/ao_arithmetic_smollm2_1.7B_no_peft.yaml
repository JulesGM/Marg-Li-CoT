# @package _global_
defaults:
  - override /output_type: answer_only

dataset_choice: 
  _args_: ["arithmetic"]

output_type:
  train_batch_size:          5
  eval_batch_size:           5
  max_length:                170

model_name_or_path: "HuggingFaceTB/SmolLM2-1.7B-Instruct"
max_num_epochs: 4

use_peft: false
